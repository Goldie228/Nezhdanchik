# 🍜 Нежданчик — виртуальный ресторан восточной кухни

<div align="center">

![Ruby](https://img.shields.io/badge/Ruby-3.2.2-red)
![Rails](https://img.shields.io/badge/Rails-7.0-red)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15-blue)
![Redis](https://img.shields.io/badge/Redis-7-orange)
![Docker](https://img.shields.io/badge/Docker-✓-blue)
![RSpec](https://img.shields.io/badge/RSpec-✓-red)
![MIT License](https://img.shields.io/badge/License-MIT-green)

Веб-приложение для полного цикла работы ресторана восточной кухни

[Описание](#описание) •
[Особенности](#особенности) •
[Быстрый старт](#быстрый-старт) •
[Документация](#документация) •
[Разработка](#разработка)

</div>

---

## 📖 Описание

**Нежданчик** — это полнофункциональное веб-приложение для управления рестораном восточной кухни, разработанное в рамках учебного проекта по дисциплине ТРПО. Приложение охватывает все аспекты работы ресторана: от бронирования столиков и онлайн-заказов до управления инвентарём и аналитики.

### 🎯 Цели проекта
- Демонстрация навыков разработки на Ruby on Rails 7
- Реализация сложной бизнес-логики бронирования и заказов
- Интеграция современных инструментов (Docker, Avo Admin, DaisyUI, RSpec)
- Создание production-ready приложения с полноценной документацией и покрытием тестами

### 👥 Команда
| Роль | ФИО | Группа |
|------|-----|--------|
| Основной разработчик | Кажуро Глеб Андреевич | Т-393 |
| Соразработчик | Джахджах Александр | Т-393 |

---

## ✨ Особенности

### 🍽️ Для клиентов
- **Интерактивное бронирование** столиков с выбором конкретных мест
- **Кастомизация блюд** — добавление/удаление ингредиентов в реальном времени
- **Умная корзина** с расчётом веса и стоимости
- **История заказов** с возможностью повторного заказа
- Двухфакторная аутентификация через email

### 👨‍🍳 Для персонала
- **Панель управления** с календарём бронирований
- **Управление заказами** в реальном времени
- **Контроль занятости столиков
- **Мобильный интерфейс** для работы в зале

### 🛠️ Технические особенности
- **Контейнеризация** — полная изоляция через Docker Compose
- **Производительность** — Redis для кеширования и сессий
- **Безопасность** — JWT-токены, защита от CSRF, SQL-инъекций
- **PWA** — установка как нативного приложения
- **Темы оформления** — выбор тем через DaisyUI
- **Админ-панель** — Avo для управления данными
- **Тестирование** — RSpec для проверки бизнес-логики

---

## 🚀 Быстрый старт

### Предварительные требования
- [Docker](https://docs.docker.com/get-docker/) и [Docker Compose](https://docs.docker.com/compose/install/)
- Git
- 4 ГБ свободной оперативной памяти

### Установка за 4 шага

1.  **Клонирование репозитория**
    ```bash
    git clone https://github.com/Goldie228/Nezhdanchik.git
    cd Nezhdanchik
    ```

2.  **Настройка окружения**
    ```bash
    cp .env.example .env
    # Отредактируйте .env при необходимости (пароли, email)
    ```

3.  **Запуск контейнеров**
    ```bash
    docker compose --profile dev up --build
    ```

4.  **Инициализация базы данных** *(произойдёт автоматически)*
    - **Важно**: Для начала работы вам нужно зарегистрировать пользователя, через `docker compose exec app bin/rails console` предоставить права доступа к админке `User.first.role = "admin"`, далее заполнить обязательные данные *(столы, стулья, категории, блюда)*

**Готово!** Приложение доступно по адресу: http://localhost:3000

---

## 📚 Документация

### 📊 Структура базы данных

#### Основные сущности:
- **Пользователи** (`users`) — клиенты, менеджеры, администраторы
- **Бронирования** (`bookings`) — резервации столиков
- **Столы и места** (`tables`, `seats`) — инфраструктура ресторана
- **Блюда и ингредиенты** (`dishes`, `ingredients`) — меню с кастомизацией
- **Заказы и корзина** (`orders`, `carts`) — система продаж

#### Важные ограничения:
- Максимальная длительность бронирования — 5 часов
- Проверка пересечений бронирований
- Расчёт стоимости в зависимости от количества мест
- Валидация доступности ингредиентов

### 🔐 Безопасность

#### Аутентификация:
- Хеширование паролей через `bcrypt`
- Двухфакторная аутентификация через email
- Сессии хранятся в Redis
- Защита от brute-force атак

#### Авторизация:
- Ролевая модель (клиент, менеджер, администратор)
- Pundit для контроля доступа
- Раздельные интерфейсы для разных ролей

### 📁 Структура проекта

```
.
├── app/                  # Основной код приложения
│   ├── assets/           # Статические ресурсы (CSS, JS, изображения)
│   ├── controllers/      # Контроллеры
│   ├── helpers/          # Помощники представлений
│   ├── models/           # Модели данных
│   └── views/            # Шаблоны представлений
├── config/               # Конфигурационные файлы
├── db/                   # Файлы миграций и схемы базы данных
├── lib/                  # Библиотеки и дополнительные модули
├── public/               # Публичные файлы
├── spec/                 # Тесты RSpec
│   ├── models/           # Тесты для моделей (основной фокус)
│   ├── requests/         # Тесты для запросов
│   └── support/          # Вспомогательные файлы для тестов
└── ...                   # Другие стандартные директории Rails
```

### 📝 Соглашения по коду

#### Ruby/Rails
- Используйте RuboCop для проверки стиля кода.
- Следуйте руководству по стилю Ruby.
- Используйте описательные имена для переменных и методов.

#### JavaScript
- Используйте ESLint для проверки стиля кода.
- Следуйте руководству по стилю Airbnb.

### 🔄 Процесс разработки

#### Ветвление
- `main` — основная ветка для продакшена.
- `develop` — ветка для разработки.
- `feature/*` — ветки для новых функций.
- `bugfix/*` — ветки для исправления ошибок.

#### Коммиты
- Используйте [Conventional Commits](https://www.conventionalcommits.org/).
- Пример: `feat(booking): add seat selection functionality`.

---

## 🛠️ Разработка

### Локальная разработка (без Docker)

```bash
# Установка Ruby (через rbenv)
rbenv install 3.2.2
rbenv local 3.2.2

# Установка зависимостей
bundle install
npm install

# Настройка базы данных
bin/rails db:create db:migrate db:seed

# Запуск
bin/dev
```

### Команды Docker

```bash
# Разработка
docker compose --profile dev up
docker compose exec app bin/rails console

# Запуск тестов
docker compose exec app bundle exec rspec

# Мониторинг
docker compose logs -f app
docker compose ps
docker stats
```

### Тестирование

Мы используем **RSpec** для тестирования. Основной фокус — на проверке бизнес-логики в **моделях**, чтобы обеспечить надежность и корректность работы ядра приложения.

```bash
# Запустить все тесты
docker compose exec app bundle exec rspec

# Запустить тесты только для моделей
docker compose exec app bundle exec rspec spec/models

# Запустить тесты для конкретного файла
docker compose exec app bundle exec rspec spec/models/user_spec.rb

# Запустить тесты с форматированным выводом
docker compose exec app bundle exec rspec --format documentation
```

#### Покрытие кода
Для генерации отчета о покрытии кода используется **SimpleCov**. Отчет автоматически создается в `coverage/` после запуска тестов.

```bash
# Запустить тесты и сгенерировать отчет
docker compose exec app bundle exec rspec

# Просмотреть отчет (откроется в браузере)
open coverage/index.html
```

### Миграции

```bash
# Создание миграции
docker compose exec app bin/rails generate migration AddIndexToUsersEmail

# Выполнение миграций
docker compose exec app bin/rails db:migrate

# Откат миграции
docker compose exec app bin/rails db:rollback STEP=1

# Состояние миграций
docker compose exec app bin/rails db:migrate:status
```

---

## 📦 Производственное развертывание

### Конфигурация продакшена

1.  **Настройка переменных окружения** (`.env.production`):
    ```env
    RAILS_ENV=production
    RAILS_SERVE_STATIC_FILES=true
    RAILS_LOG_TO_STDOUT=true
    DATABASE_URL=postgresql://user:password@host:5432/database
    REDIS_URL=redis://:password@host:6379/1
    SECRET_KEY_BASE=your-secret-key-base
    ```

2.  **Сборка образа**:
    ```bash
    docker build -t your-registry/nezhdanchik:latest .
    docker push your-registry/nezhdanchik:latest
    ```

3.  **Запуск в продакшене**:
    ```bash
    docker run -d \
      --name nezhdanchik \
      -p 3001:3000 \
      --env-file .env.production \
      --restart unless-stopped \
      your-registry/nezhdanchik:latest
    ```

### Мониторинг и логи

```bash
# Просмотр логов
docker logs -f nezhdanchik

# Мониторинг ресурсов
docker stats nezhdanchik

# Health check
curl http://localhost:3001/up
```

#### Мониторинг
- Используйте Prometheus для сбора метрик.
- Используйте Grafana для визуализации метрик.
- Настройте оповещения о критических ошибках.

#### Бэкапы
- Ежедневные бэкапы базы данных.
- Хранение бэкапов в течение 30 дней.
- Автоматическое восстановление при необходимости.

---

## 🔧 Устранение неполадок

### Частые проблемы и решения

#### Проблема: Стили не загружаются
```bash
# Очистка кеша
docker compose exec app bin/rails assets:clobber
docker compose exec app bin/rails tailwindcss:build

# Проверка сборки
docker compose exec app ls -la app/assets/builds/
```

#### Проблема: Ошибки базы данных
```bash
# Восстановление базы
docker compose down -v
docker compose up -d db
docker compose exec app bin/rails db:setup

# Проверка соединения
docker compose exec app bin/rails runner "puts ActiveRecord::Base.connection.active?"
```

#### Проблема: Контейнер не запускается
```bash
# Проверка логов
docker compose logs --tail=50 app

# Проверка портов
netstat -tulpn | grep :3000

# Пересборка образов
docker compose build --no-cache
```

#### Проблема: Redis соединение
```bash
# Тестирование Redis
docker compose exec redis redis-cli ping

# Очистка кеша
docker compose exec redis redis-cli flushall
```

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробности смотрите в файле [LICENSE](LICENSE).

---

## 🤝 Вклад в проект

Мы приветствуем вклад в развитие проекта! Пожалуйста, следуйте этим шагам:

1.  **Форкните репозиторий**
2.  **Создайте ветку для фичи** (`git checkout -b feature/amazing-feature`)
3.  **Зафиксируйте изменения** (`git commit -m 'Add amazing feature'`)
4.  **Запушьте в ветку** (`git push origin feature/amazing-feature`)
5.  **Откройте Pull Request**

### Руководство по коммитам
- Используйте [Conventional Commits](https://www.conventionalcommits.org/).
- Добавляйте **RSpec-тесты** для новой бизнес-логики (в основном в `spec/models`).
- Обновляйте документацию при изменении API.

---

## 📞 Контакты и поддержка

### Основной разработчик
- **Кажуро Глеб Андреевич**
- Email: [glebkazhuro@gmail.com](mailto:glebkazhuro@gmail.com)
- GitHub: [@Goldie228](https://github.com/Goldie228)

### Соразработчик
- **Джахджах Александр**
- Группа: Т-393

### Сообщить о проблеме
- [GitHub Issues](https://github.com/Goldie228/Nezhdanchik/issues)
- При создании issue укажите:
    - Версию Rails и Ruby.
    - Логи ошибок.
    - Шаги для воспроизведения.
    - Ожидаемое поведение.

---

<div align="center">

## ⭐ Если проект вам понравился, поставьте звезду на GitHub!

**Спасибо за внимание! Приятного использования!** 🎉

</div>
